# Consolidation tools -- allowlist only (fallback not needed, local DictInjector)

tools:
  deprecate_l1_rule:
    timeout: 5
    allowlist: [l1]
    fallback:
      max_retries: 0
      degrade: []

  create_l1_rule:
    timeout: 5
    allowlist: [l1]
    fallback:
      max_retries: 0
      degrade: []

  modify_l1_rule:
    timeout: 5
    allowlist: [l1]
    fallback:
      max_retries: 0
      degrade: []

  deprecate_l2_card:
    timeout: 5
    allowlist: [l2]
    fallback:
      max_retries: 0
      degrade: []

  create_l2_card:
    timeout: 5
    allowlist: [l2]
    fallback:
      max_retries: 0
      degrade: []

  modify_l2_card:
    timeout: 5
    allowlist: [l2]
    fallback:
      max_retries: 0
      degrade: []

  deprecate_l3_skill:
    timeout: 5
    allowlist: [l3]
    fallback:
      max_retries: 0
      degrade: []

  create_l3_skill:
    timeout: 5
    allowlist: [l3]
    fallback:
      max_retries: 0
      degrade: []

  modify_l3_skill:
    timeout: 5
    allowlist: [l3]
    fallback:
      max_retries: 0
      degrade: []
