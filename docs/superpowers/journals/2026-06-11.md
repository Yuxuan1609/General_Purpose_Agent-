## 2026-06-11 — Tool System Review

### Tool Registration Consolidation
- Removed `skills_list`/`skill_view`/`skill_manage` from SkillLayer (L3 tools injected via ToolRegistry, not per-layer)
- Moved `read_file`/`grep` from `capability/example_tools.py` → `core/tools/file_tools.py`
- Created `core/tools/tool_proposal.py` — agent proposes new tools, saved to `data/tool_proposals/` for human review
- Created `core/tools/__init__.py` with `register_all_tools(registry, proposal_dir)` as unified mount point
- Added `_mount_tools()` to `chain_factory.build_default_chain()` — all agents get tools regardless of env

### Tool Allowlist Update
- Removed skill tools from `DEFAULT_TOOL_ALLOWLIST`
- Extended `web_search` to L2
- Added `tool_proposal` to L2/L3
- L1 only has `todo`

### Bugfix: Tools Never Passed to LLM
- Root cause: `_mount_tools()` correctly set `LayerInjector` via `set_injector()`, but no layer's `stage*()` methods ever passed `tools=` to `_call_llm()`
- Fix: Added `LayerAgent._get_tools(layer)` → injects tools from injector
- Updated L1 stage1/stage2, L2 stage1/stage2, L3 execute to call `_get_tools()`
- Consolidation paths unchanged (use hardcoded `_Lx_CONSOLIDATION_TOOLS`)

### System Prompt Updates
- L1: clarified "不直接调用工具，需要工具的任务下发给 L2/L3"
- L2: added domain overview (`## 领域总览`) + tool capability description
- L3: added tool capability description

### Data Cleanup
- Trimmed `l1_rules.json` from 12 rules → 4 seed rules
- Removed 16 fixture skills, kept 3 seed skills
- `.gitignore`: added `data/learning/pending/`, `data/interaction/`, `data/tool_proposals/`

### DouZero History
- `_build_state()` now formats `card_play_action_seq` as per-round play history

### Learning Env
- `build_task_observation()` now populates `history` with conversation-format
