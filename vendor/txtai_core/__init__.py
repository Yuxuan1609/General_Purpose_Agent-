"""Vendor-forked txtai core modules.

Stripped to: embeddings, graph, database (SQLite-only), scoring, vectors,
archive, ann, serialize, util.

Targeting DeepSeek-compatible API (no HuggingFace, no cloud services).
"""
